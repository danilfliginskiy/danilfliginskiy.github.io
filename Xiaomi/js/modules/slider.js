$(document).ready(function () {
	$('.carousel').slick({
		speed: 1000,
		slidesToShow: 1,
		centerMode: false,
		infinite: false,
		slidesToScroll: 1,
		variableWidth: true,
		dots: true,
	})
})

function carousel() {}

export default carousel
